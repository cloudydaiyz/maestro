/** Controller function to handle routing */
type Controller = (path: string, body: any) => void;

export const apiController: Controller = () => {
    // do something
}

export const syncController: Controller = () => {
    // do something
}